{
  "identifier": "default",
  "description": "Default capability for AlgoMentor GUI",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "dialog:allow-open",
    "dialog:allow-save"
  ]
}
